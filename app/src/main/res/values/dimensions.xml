<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Margin around the floating action button in CatalogActivity -->
    <dimen name="fab_margin">16dp</dimen>

    <!-- Common margin value used throughout the app -->
    <dimen name="activity_margin">16dp</dimen>

    <!-- Image Size -->
    <dimen name="image_size">88dp</dimen>

    <!-- Icon Size -->
    <dimen name="icon_size">24dp</dimen>

    <!-- Icon Touch Target Size -->
    <dimen name="icon_touch_target_size">48dp</dimen>

    <!-- Button Size -->
    <dimen name="button_height">36dp</dimen>

    <!-- Button Touch Target Size -->
    <dimen name="button_touch_target_height">48dp</dimen>

    <!-- Smaller margin within the Catalog Layout -->
    <dimen name="catalog_smaller_margin">8dp</dimen>

    <!-- Smaller margin within the List Item Layout -->
    <dimen name="item_smaller_margin">4dp</dimen>

    <!-- Small margin within the Editor Layout -->
    <dimen name="editor_small_margin">12dp</dimen>

    <!-- Smaller margin within the Editor Layout -->
    <dimen name="editor_smaller_margin">4dp</dimen>

    <!-- Order Button padding within the Editor Layout -->
    <dimen name="editor_button_padding">8dp</dimen>

    <!-- Order Button corner radius -->
    <dimen name="button_corner_radius">4dp</dimen>

</resources>